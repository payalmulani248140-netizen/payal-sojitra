<div class="custom-product-grid">
    <div class="container">
        <div class="product-box">
            {% for block in section.blocks %}
                <div class="look-item">
                    {%- if block.settings.image != blank -%}
                        {% assign product_image = block.settings.image %}
                    {% else %}
                        {% assign product_image = product.featured_image %}
                    {% endif %}

                    {%- if product_image != blank -%}
                        <img
                            src="{{ product_image | image_url: width: 900 }}"
                            srcset="
                            {{ product_image | image_url: width: 375 }} 375w,
                            {{ product_image | image_url: width: 550 }} 550w,
                            {{ product_image | image_url: width: 750 }} 750w,
                            {{ product_image | image_url: width: 900 }} 900w,
                            {{ product_image | image_url: width: 1200 }} 1200w,
                            {{ product_image | image_url: width: 1500 }} 1500w,
                            {{ product_image | image_url: width: 1800 }} 1800w
                            "
                            sizes="100vw"
                            width="{{ product_image.width }}"
                            height="{{ product_image.height }}"
                            alt="{{ product_image.alt | escape }}"
                            loading="eager"
                            fetchpriority="high"
                        >
                        {%- endif -%}

                    <button
                    class="look-icon"
                    style="
                    left:{{ block.settings.icon_x }}%;
                    top:{{ block.settings.icon_y }}%;
                    "
                    data-product="{{ all_products[block.settings.product].handle }}">
                    +
                    </button>

                </div>
            {% endfor %}
        </div>
    </div>
</div>

{% schema %}
{
  "name": "Custom Product Grid",
  "settings": [
    {
      "type": "range",
      "id": "columns",
      "label": "Desktop Columns",
      "min": 2,
      "max": 4,
      "step": 1,
      "default": 3
    }
  ],
  "blocks": [
    {
      "type": "look",
      "name": "Look Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Look Image"
        },
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        },
        {
          "type": "range",
          "id": "icon_x",
          "label": "Icon Horizontal Position",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        },
        {
          "type": "range",
          "id": "icon_y",
          "label": "Icon Vertical Position",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 50
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Product Grid"
    }
  ]
}
{% endschema %}